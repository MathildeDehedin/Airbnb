import React from "react";
import { ActivityIndicator, View } from "react-native";

const Activityindictor = () => {
  return (
    <View style={{ flex: 1 }}>
      <ActivityIndicator size="large" color="red" />
    </View>
  );
};
export default Activityindictor;
